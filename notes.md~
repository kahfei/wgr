# Chapter 1
```ruby
irb --simple-prompt 
```
Alt-Some fundemental stuffs
`puts` add a newline, `print` doesn't, `p` outputs and inspect string
`=` is to assign, `==` is to compare
`self` refer to default object, depending on execution context


